import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DetalledietaPage } from './detalledieta';

@NgModule({
  declarations: [
    //DetalledietaPage,
  ],
  imports: [
    IonicPageModule.forChild(DetalledietaPage),
  ],
})
export class DetalledietaPageModule {}
